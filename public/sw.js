// This is a basic service worker file.
// It's required for a PWA to be installable.

self.addEventListener('fetch', (event) => {
  // We are not adding any caching logic for now.
  // This just makes the app installable.
});
